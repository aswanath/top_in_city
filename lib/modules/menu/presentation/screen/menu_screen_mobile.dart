import 'package:flutter/material.dart';

class MenuScreenMobile extends StatelessWidget {
  const MenuScreenMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
