import 'package:flutter/material.dart';

class CoreScreenMobile extends StatelessWidget {
  const CoreScreenMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
